# Secrets / runtime
.env
.env.local
.env.*.local
*.p12
*.pem
*.key
secrets/
certs/

# Go
apps/api/bin/
apps/api/tmp/
*.test
*.out
vendor/

# Node / PWA
node_modules/
apps/web/dist/
apps/web/.vite/
apps/web/tsconfig.tsbuildinfo
*.log
.DS_Store

# Capacitor: las plataformas nativas se generan localmente con
# `npx cap add android` (requiere Android SDK). No las commiteamos para
# evitar diffs gigantes; quien quiera buildear la APK lo regenera.
apps/web/android/
apps/web/ios/

# PHP / motor
vendor/
apps/motor/vendor/
apps/motor/.phpunit.result.cache
apps/motor/storage/logs/*
apps/motor/storage/cache/*

# Datos emitidos (XML firmados, CDR, registros) — son del usuario, nunca del repo
data/

# IDE
.idea/
.vscode/
*.swp

# Postgres / Redis volumes (cuando compose monta locales)
data/
