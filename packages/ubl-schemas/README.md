# UBL 2.1 — XSDs SUNAT

XSDs oficiales que SUNAT exige para validar los XML antes de enviarlos. La fuente está en el portal del SEE en SUNAT.

Esta carpeta queda vacía en el repo a propósito — los XSDs son archivos de SUNAT y los descargamos en build time desde un mirror controlado (o se versionan acá si decidimos vendor-izarlos).

TODO: definir si vendor-izamos o si descargamos en CI. Vendor-izar es más reproducible; descargar es más limpio si SUNAT actualiza.

(Por completar.)
